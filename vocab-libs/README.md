# 社区词库

这些词库通过 App 的“词库 → 从 GitHub 下载词库”获取。

当前包是可自由再分发的 starter 词库，主要用于验证远程下载流程；词条、释义和来源说明写在各 JSON 文件中。教材型完整词表请贡献者确认授权后再提交，禁止上传来源不明的商业词库。

## 文件格式

```json
[{"w":"word","m":[["n.","释义"]]}]
```

新增词库后，请在 `catalog.json` 增加条目。App 会自动读取目录，不需要重新编译。
